let num = prompt("Quantos numero a lista tera")
let lista =[];
let digitado = 0
for(i=0;i<num;i++){
    digitado = prompt("digite um numero")
    lista.push(digitado)
}
console.log(lista)
let rep = prompt("Qual numero vc deseja verifcar quantas vezes foi digitado")
for(let i=0;i<num;i++){
if(lista[i]=rep){
    
}} console.log(quant)